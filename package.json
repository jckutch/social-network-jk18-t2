{
  "name": "social-network-jk18-t2",
  "version": "1.0.0",
  "description": "API for a social network that uses a NoSQL database",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.9.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.9"
  }
}
